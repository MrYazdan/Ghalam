[project]
name = "ghalam"
version = "0.1.0"
description = "Ghalam (قلم) - Fast, elegant screen drawing & annotation tool for Linux"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.6.0",
]

[project.scripts]
ghalam = "ghalam.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
